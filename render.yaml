services:
  # Backend Service
  - type: web
    name: kyc-backend
    env: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: FRONTEND_URL
        value: https://kyc-frontend.onrender.com
    healthCheckPath: /health

  # Frontend Service
  - type: web
    name: kyc-frontend
    env: static
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: frontend/build
    # SPA fallback: direct hits (e.g. Clerk redirect to /admin?__clerk_handshake=…) must serve index.html
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
    envVars:
      - key: REACT_APP_API_URL
        value: https://kyc-backend.onrender.com
